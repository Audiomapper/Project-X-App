import React from 'react';
import {
  Text
} from 'react-native';

// import { signOut } from '../../utils/authorizationToken';

const Dashboard = () => (
  <Text>Dashboard</Text>
);

export default Dashboard;
